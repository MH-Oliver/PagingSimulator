# ------------------------------------------------------------------------------
# Project: Paging Simulator (Qt UI + C++ Core)
# Purpose: Ignore build/IDE/generated outputs across the repo (and subfolders).
# ------------------------------------------------------------------------------

# --- Doxygen outputs ---
docs/html/
docs/latex/
docs/doxygen_warnings.log

# --- Qt / qmake generated (recursive) ---
**/*.pro.user
**/*.user
**/moc_*
**/ui_*
**/qrc_*
**/.qmake.stash
**/Makefile*
**/*.qrc.dep

# --- CMake / build artifacts (recursive) ---
**/build/
**/cmake-build-*/
**/CMakeFiles/
**/CMakeCache.txt
**/cmake_install.cmake
**/CTestTestfile.cmake
**/install_manifest.txt
**/*_autogen/
# Common generators
**/compile_commands.json

# --- Objects / libraries / binaries (recursive) ---
**/*.o
**/*.obj
**/*.lo
**/*.la
**/*.a
**/*.lib
**/*.dll
**/*.so
**/*.dylib
**/*.exe
**/*.pdb
**/*.ilk

# --- IDE / Editors (recursive) ---
**/.idea/
**/.vscode/
**/*.iml

# --- OS junk (recursive) ---
**/.DS_Store
**/Thumbs.db
**/desktop.ini

# --- Logs / temp (recursive) ---
**/*.log
**/*.tmp
**/~$*

# Keep runtime resources and configs versioned:
# (do NOT ignore these)
!resources/
!Doxyfile
!.clang-format
!.editorconfig
!.gitmodules
